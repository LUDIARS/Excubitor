{
  "name": "env-management-and-injection",
  "description": "Environment variable resolution, topology env generation, Infisical mapping, secret agent resolution, and domain-root settings.",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    { "pathPattern": "(^|/)src/secrets/[^/]+$" },
    { "pathPattern": "(^|/)src/process/(inject|startup-env|topology)\\.ts$" },
    { "pathPattern": "(^|/)src/shared/roots(\\.test)?\\.ts$" },
    { "pathPattern": "(^|/)src/catalog/(loader|sync|watcher|editor|auto-catalog|auto-catalog-file|ports|tier)\\.ts$" },
    { "pathPattern": "(^|/)src/mcp/mcp-json\\.ts$" },
    { "pathPattern": "(^|/)catalog/services\\.ya?ml$" },
    { "pathPattern": "(^|/)frontend/src/pages/(Catalog|Config)\\.tsx$" },
    { "pathPattern": "(^|/)src/index\\.ts$", "namePattern": "reloadCatalog" }
  ],
  "cardTemplate": "Summarise the env-management-and-injection domain: catalog/env sources, Infisical overrides, secret resolution, topology env, and startup validation."
}
